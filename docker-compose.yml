version: '3'
services:
  front:
    build:
      context: ./frontend
      dockerfile: Dockerfile
  back:
    build:
      context: ./backend
      dockerfile: Dockerfile
  db:
    build:
      context: ./db
      dockerfile: Dockerfile
